

---

## 7. 実装方針の具体化（2026-07-27改訂 — 初期分析完了を受けて）

ユーザー指定の3論点＋補完点を、実装可能な形に落とす。九州・北海道の初期分析で検証済みの部品（供給曲線推定=07、帯域分解=08、DP運用=03、MC=04、市場制度=notes/13）を再利用する。

### 7.1 ①価格過程のモデル化（太陽光・風力導入量 K_pv・K_wind を変数に）

- **構造ルート（主）**: p_t = g(net_t; 季節, レジーム)、net_t = D_t − K_pv·cf_pv,t − K_wind·cf_w,t − 原子力_t
  - cf（設備利用率）過程: 実測フリート出力を導入量系列で正規化して抽出し、**時間構造（帯域分布・自己相関）を保存したブートストラップ**で将来パスを生成。帯域分解（h6）が保存すべき統計量の検証器になる
  - g: FY2023-25の実測から季節別に推定（07の方式）。スパイク側はPirrong型関数形、下側は**0.01円打ち切り（センサリング）を明示**
  - **市場分断の2レジーム**: 北本フローが運用容量に張り付くコマ（分断）とそれ以外で g を分ける — 北海道では一級の構造（h5: 分断が価格の3割）。連系線増強シナリオはレジーム頻度の変化として入る
- **誘導形ルート（検証用）**: h7の分位点回帰・HAR-X（第1段階）で構造ルートの弾力性をクロスチェック
- 検証: FY2023-25の床コマ・TB4h・帯域構造の再現 → FY2026でアウトオブサンプル

### 7.2 ②3市場の有機的つながり（notes/13 §4 の実装）

- ゼロ利潤条件: **π_spot(K; K_pv, K_wind, s) ＋ κ·P_cap(K; s) − c_req ＝ AFC(h)**
- スポット: ①の価格過程上で蓄電池フリートのDP運用。**フリートの充放電を net_t に還流させて g を通す**ことで価格インパクト（カニバリ）を内生化（Butters の単一エージェント化＋flow return surface近似、Kグリッド数点×回帰面）
- 容量市場: P_cap(K) は**公表需要曲線から閉形式で計算**（推定不要）。目標調達量は需要シナリオと連動。κ=調整係数（要一次確認）
- 需給調整: 機会費用リンクにより均衡ではゼロレント（ベース）。移行期レントは感応度。同時市場化（2030年代前半）が長期整合性の根拠
- LTDA落札分は政策ストック K_policy として初期Kに算入（京極揚水と同様）

### 7.3 ③シナリオ分析（§3.2の行列を継続。モデル上の入り方を明記）

- 泊再稼働: net_t から91.2万kWを控除（稼働率カレンダー付き）。**両方向に効く（余剰↑×スパイク↓）＝符号は定量化でしか決まらない** — 九州FY2023の「原発×太陽光→制御急増」が予行演習
- DC・半導体需要: フラット負荷 δD の加算（07の反実仮想と同じ機構）。九州の教訓: フラット負荷は床を増やしても**価値には中立的** → 「DC需要はK*をほぼ毀損しない（むしろ逼迫側で押し上げ）」が検証可能な仮説
- 洋上風力: K_wind の増加＋cf過程の変化（洋上は平滑だが相関が強い）を別パラメータで
- 北本120万kW/HVDC: 分断レジーム頻度の変化として

### 7.4 補完すべき重要ポイント（ユーザー3点への追加指摘）

1. **資本費 AFC(h) の精緻化**（ゼロ利潤条件の右辺）: LTDA落札価格＝市場実測の年間固定費を軸に、duration構造 AFC(h)=a+b·h とコスト低下経路 c_y を整備
2. **duration h の内生化**（新規性の核）: 収益は帯域構造ゆえ h に対して凹、費用は線形 → 内点解 h* が存在。K*(h) 曲線で「北海道の均衡durationは4hか6h以上か」に答える
3. **蓄電池の価格インパクト実装**が計算の心臓部: これがないと π(K) が平坦になり K* が発散する。Kグリッド×flow return surface で近似
4. **既存フレキシビリティの内生応答**: 揚水（京極）・北本フロー・（九州比較で分かった）需要側吸収は価格に応答する。少なくとも揚水と連系線は運用ルールベースで内生化、入札行動変化は限界として明示
5. **検証戦略の設計**: (i) in-sample再現、(ii) FY2026アウトオブサンプル、(iii) CAISO/NEMのカニバリ速度との比較、(iv) 2027年泊再稼働・新設蓄電池COD（札幌50MW級）のイベントスタディ — 論文執筆前に自然実験が2つ起きる
6. **下限0.01円とマイナス価格制度変更**: 推定はセンサリング処理、制度変更はシナリオスイッチ（下方開放は蓄電池価値に非対称にプラス）
7. **リスクと資本コスト（金融工学の顔）**: 年度間の収益ドリフト（MCの過小分散で確認済み）→ 要求リターン・ファイナンス可能性が AFC を動かす。K*(WACC) の感応度と、参入タイミングの最適停止（待つオプション）
8. **東北パネルの活用**: 「風力大×連系線太い」対照エリアとして、分断閉じ込めメカニズムの識別に使う（頑健性）

### 7.5 実装フェーズ（目安）

| フェーズ | 内容 | 主な既存部品 |
|---|---|---|
| A. 基盤データ | 北海道の太陽光・風力導入量の四半期系列（cf正規化用）／AFC素材（LTDA・算定委）／容量市場需要曲線・調整係数 | notes/13・14の残タスク |
| B. 価格過程v1 | 供給曲線g（2レジーム・センサリング）＋cf過程＋検証 | 07・08・h5-h7 |
| C. 運用とカニバリ | DP運用＋フリート還流＋π(K)曲線（Kグリッド） | 03・Butters再現パッケージ |
| D. 3市場統合とK* | ＋κ·P_cap(K)−c_req＝AFC(h) の求解 | notes/13 |
| E. シナリオ×duration | K*(s)行列・K*(h)・ロバスト下限 → 執筆 | §3.2 |
